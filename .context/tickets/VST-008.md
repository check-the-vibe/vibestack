# VST-008 — Operation inventory and shared API contract

- Phase: in_progress
- Specification: [SPEC-002](../specifications/SPEC-002-rest-api-agent-distribution.md)
- Initiative: [Agent platform](../initiatives/agent-platform.md)
- Owner: Codex
- Branch manifest: [VST-008](../branches/VST-008.md)
- Depends on: none
- Updated: 2026-09-21

## Outcome and scope

Audit actual operations and define the small capability registration, route/CLI mapping, authentication context, errors, versions and per-operation retry contract. Map every capability across CLI, REST, authenticated MCP and web services with shared authorization/semantics and explicit unavailable or human-handoff states. Use one harmless extension to validate the proposed shape. This is a contract task; it does not require a broker or deployment.

## Acceptance

- [ ] SPEC-002 AC-01: verify the ticket-owned portion below and link the result.
- [ ] Documentation, compatibility behavior and exact validation evidence match the implemented change.

## Development plan

1. Audit `api/workspace.openapi.json`, `api/runner.openapi.json`, `api/command-coverage.json`, `automation/`, `control/`, `runner/mcp.go`, `nginx.conf` and `cmd/vibestack/` against their actual handlers. Separate workspace authority from optional host-runner authority.
2. Record a machine-readable capability inventory with CLI/REST/MCP/web mappings, implementation status, human handoffs, limits, retry rules, source evidence and test coverage. Include setup/control operations missing from the existing OpenAPI/CLI coverage file.
3. Write `docs/architecture/capability-contract.md` to resolve SPEC-002 D1 and SPEC-001 D2. Define the smallest shared registration shape; validate it with one harmless example capability. Do not introduce fleet routing or a second agent loop.
4. Add meaningful schema/coverage drift checks if this pass adds executable contract tooling. Run repository source checks before and after source changes; use the documentation exception only for a strictly documentary pass.
5. Record precise acceptance evidence. Push the ticket-owned branch within the user's authorization; no new PR is requested for this preparation pass.

## Verification scope

Validate schemas and each mapping against source, trace every capability to authority and acceptance, and review compatibility/missing operations.

## Evidence

Branch preparation only so far: the inventory, contract and schema checks above
remain to be built. Read the current Codespaces verification record below for
environment evidence; that is not capability-contract acceptance.

## Decisions and progress

- 2026-09-21: Created during [VST-004](VST-004.md). Entry decisions: Resolve SPEC-002 D1 in this task; coordinate the single-workspace service and trusted-extension assumptions in SPEC-001. Record recommendations as decisions before dependent implementation.

- 2026-09-21: Aligned planned scope/dependencies with the simplified SPEC-001 service host; retired machine-broker prerequisites. No implementation claimed.

- 2026-09-21: User authorized integrating the existing work directly into main, creating a fresh Codespace in Chrome, then reusing it on the next ticket branch. Claimed this ticket on `codex/vst-008-capability-contract` from main at `2cfa9d8`; no new PR was opened.

## Codespace preparation

Fresh Codespace: [Supreme Space Parakeet editor](https://supreme-space-parakeet-p7jgg5v9jqh696v.github.dev/).
[Desktop](https://supreme-space-parakeet-p7jgg5v9jqh696v-8080.app.github.dev/vnc/?view=desktop).
Created from main at `aa8fe5b`, with 4 cores and 15,993 MiB RAM. Lifecycle completed
with exit 0 and `VibeStack ready`; Chrome showed Connected. The Ports panel
confirmed 8080 Private and Docker proxy binding 127.0.0.1. The startup log reported
the writable `/workspaces/vibestack` to `/projects/vibestack` source mount.
The non-mutating live check passed: 71 passed, 0 failed, 2 skipped.
Branch-switch/reuse verification is still in progress and will be recorded here.
This new instance has its own persistent state; old Codespace state was not copied.

## Handoff

- Remaining work: operation inventory, contract decisions, harmless extension example and drift validation. No capability implementation is claimed.
- Next action: run the development prompt for VST-008 and follow steps 1–3 above. Start with actual routes/handlers, rather than implementing a new service prematurely.
- Branch: `codex/vst-008-capability-contract`; base main at `2cfa9d8`.
- Working directory in Codespaces: `/workspaces/vibestack`; same source inside VibeStack: `/projects/vibestack`.
- Uncommitted work: none after the branch-preparation commit; inspect status before the next pass.
- Delivery: this ticket is in flight on its branch; main still has its planned record.
