# VST-006 — Capability registration and extension authoring

- Phase: in_review
- Specification: [SPEC-001](../specifications/SPEC-001-service-host.md)
- Initiative: [Agent platform](../initiatives/agent-platform.md)
- Owner: Codex
- Branch manifest: [VST-006](../branches/VST-006.md)
- Depends on: [VST-005](VST-005.md), [VST-008](VST-008.md)
- Updated: 2026-09-21

## Outcome and scope

Build the small capability definition/registration API and common dispatcher. Supply a harmless example module, REST adapter mapping and metadata consumed by the MCP/CLI adapters. Validate schema, permissions, route/name uniqueness and limits. Do not build dynamic plugin loading or a code-upload API.

## Acceptance

- [ ] SPEC-001 AC-02: verify the ticket-owned portion below and link the result.
- [ ] SPEC-001 AC-03: verify the ticket-owned portion below and link the result.
- [ ] Documentation, compatibility behavior and exact validation evidence match the implemented change.

## Development plan

1. Read the service-host specification and shared capability contract.
2. Implement this bounded increment, preserving existing private clients/data.
3. Run focused checks and the repository gates required for source/runtime changes.
4. Record evidence for this ticket's part of each criterion; integration criteria remain open until tested with real clients.

## Verification scope

Add/remove an example capability without changing nginx or authentication. Check dispatch authorization, invalid/duplicate registration rejection and REST schema/results. MCP and generic CLI proof is completed with VST-012/VST-010 and recorded in VST-007.

## Evidence

Source baseline passed (`/tmp/vst-006-before.log`). The compiled registry and
project summary handler run through authenticated REST/browser sessions. Focused
Go tests pass for registration/schema/grant/output failures, bounded capacity and
timeouts, panic sanitization, project symlinks and session revocation. Image and
hosted acceptance remain pending. MCP and dedicated CLI commands are not claimed.

The source gate passed again after integration of VST-005 (312 Python tests,
all Go packages, three JS tests, syntax/JSON/whitespace). Race-enabled service
and registry tests passed. Logs: `/tmp/vst-006-verified-source.log`,
`/tmp/vst-006-race-final.log`. Context/document link and ownership checks passed.

The first image demonstrated the actual nginx extension path, matching friendly
and generic results, generated schema and denial checks. Final startup review
also rejects write effects on GET/HEAD, non-scalar query mappings, undeclared
nested object policies and excessive registration metadata. The final candidate
`vibestack:vst-006-validated-20260921`, manifest
`sha256:bc28d49ced017b0f24d30ca1f1733df26dbdd590884385a1d40371e8629fc57b`,
is running full disposable acceptance on loopback 18321. A running gate is not
a pass. Log: `/tmp/vst-006-accept-validated.log`.

## Decisions and progress

- 2026-09-21: Created during [VST-004](VST-004.md), initially for outbound connector and typed machine routing.
- 2026-09-21: Rescoped before implementation after the user replaced the fleet-broker design with a static/API/MCP service host. The previous scope is retired, not completed. Entry decisions: SPEC-001 D2 registration shape and SPEC-002 D1 shared contract; explicit trusted-extension authoring workflow.

## Handoff

- Remaining work: finish full acceptance of the final registry candidate.
- Next action: record its result, then integrate within the user's authorization.
- Branch: `codex/vst-006-capability-registry`; exact base is in its manifest.
- Uncommitted work: inspect status; registry, adapter, tests and docs belong here.
- Delivery: in flight; no registry deployment claimed.
