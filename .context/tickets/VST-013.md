# VST-013 — MCP transport recovery and compatibility

- Phase: in_review
- Specification: [SPEC-003](../specifications/SPEC-003-authenticated-mcp.md)
- Initiative: [Agent platform](../initiatives/agent-platform.md)
- Owner: Codex
- Branch manifest: [VST-013](../branches/VST-013.md)
- Depends on: [VST-012](VST-012.md), [VST-010](VST-010.md)
- Updated: 2026-09-22

## Outcome and scope

Verify remote streaming/retries/cancellation through nginx and migration for existing private clients. Fix parity gaps without adding an independent policy engine.

## Acceptance

- [x] SPEC-003 AC-05: real nginx fault-injection acceptance below.
- [x] SPEC-003 AC-06: historical/current private-runner clients and disposable image rollback below.
- [ ] Documentation, compatibility behavior and exact validation evidence match the implemented change.

## Development plan

1. Read the specification and dependencies; resolve the decisions listed below before dependent work.
2. Inspect source and capture the current contract; implement only this ticket's bounded outcome.
3. Run focused checks, then the applicable full repository gates for source/runtime changes.
4. Record acceptance evidence and remaining limits; integrate under the user's
   explicit direct-push/merge instruction without a new PR.

## Verification scope

Dropped streams, oversized output, cancellation with running jobs, duplicate mutation attempts, client/version matrix and service rollback and the separate existing private runner remaining unchanged.

## Evidence

Baseline: the exact parent `b6e443e` passed `bin/vibestack-dev test` inside Supreme
Space Parakeet, with the log at `/vibestack-runtime/vibestack/source-vst010.log`.
Its real four-client matrix, historical 0.2/current 0.3 CLI fixture and full
parent image acceptance passed locally and in the Codespace. VST-010 records
the exact accepted hosted deployment and successful live check.

Implemented wire bounds shared by workspace HTTP and source stdio: 24 MiB
frames, 256-byte encoded request IDs including escaping, and 512 bytes reserved
around tool results. A reproduced oversized-ID reflection gap is closed before
dispatch. nginx upstream replay is explicitly disabled. Operation policy and
durable jobs still use the existing dispatcher/backend.

- Passed in-memory real HTTP-handler tests for oversized/escaping-expanded IDs
  and a near-limit result that needs wrapper space. Passed actual stdio transport
  frame/ID limits and bounded HTTP-body reader tests. These require no listening
  sockets and ran with ordinary workspace permissions.
- Passed unchanged private-runner ownership/mode/revocation tests in memory:
  `TestSharedAuthorityAndModeTransitions`, `TestLegacyPairedRegistryCannotBecomeTrusted`
  and `TestMCPPairedAuthorizationAndOwnerIsolation`. Runtime `runner/` source and
  its SDK dependency remain unchanged from the pinned historical CLI base.
- Added real nginx fault-injection acceptance for dropped accepted replies from
  HTTP MCP, actual CLI stdio and generic CLI; partial JSON replies; cancellation
  after job acceptance followed by explicit cancellation; intentional duplicate
  commands; uncertain conditional writes; and truncated/bounded job output.
  All cases passed against the real nginx candidate in full image acceptance.
- Added a native old/current CLI plus SDK private-runner fixture with paired and
  explicit trusted-tailnet modes and persisted-store reopen. It requires the
  explicit historical CLI path during acceptance. Both real binaries and the
  SDK passed in both authority modes, including persisted-store reopen.
- Added optional candidate → previously accepted image → candidate acceptance,
  reusing only disposable mounts. It checks restored catalog/password readiness,
  identity, existing credentials, file bytes/ETag and job metadata. Script syntax
  and both actual image transitions passed with restored catalog/password
  readiness and the existing service credential.
- Full source and image acceptance of `937ac1a` passed. After patch-release
  alignment, the full source gate passed again at
  `/tmp/vst-013-release-source.log`. The aligned 0.3.1 image, native release publication and
  hosted deployment subsequently passed as recorded below. Earlier automatic approval timeouts cleared;
  successful execution, rather than elapsed time, establishes these new results.

## Decisions and progress

- The complete local acceptance of implementation `937ac1a` passed (exit 0):
  runtime/restoration, both remote SDKs, actual CLI stdio, guide bootstrap,
  four-client parity, all new fault cases, historical/current private-runner
  binaries, 50 browser tests and both directions of the disposable image
  rollback. Identity, existing credentials, project bytes/ETag and completed
  job metadata survived. Log: `/tmp/vst-013-accept.log`; candidate image:
  `sha256:23a77c8e78b59058a953b60378877489dbad9e612bc3daf4d241388147649a81`.
- Published [0.3.1](https://github.com/check-the-vibe/vibestack/releases/tag/v0.3.1)
  from `88bee6549e451fc9aa9cff8f11ae9fa64fe446b4`. The
  [native release workflow](https://github.com/check-the-vibe/vibestack/actions/runs/35664705236)
  passed all four Linux/macOS architecture checks, attestations and publication.
  The public installer matched the reviewed source. Fresh install and upgrade
  from public 0.3.0 produced the same Linux amd64 executable, SHA-256
  `b54ed3e8db89bb531160cdefc88fd5ab4475723203eb3da8294ee467649d5cb9`,
  which executed and reported 0.3.1. Existing release assets/tags were preserved.
- Exact 0.3.1 local candidate
  `sha256:222c1b4d9816b0f24a07996e2accbb6ae5c40afee626d084654318dae64a39ad`
  passed full acceptance including 50 browser tests, historical clients and both
  rollback transitions (`/tmp/vst-013-release-accept.log`).
- The same source passed full source/build/acceptance in Supreme Space Parakeet.
  After reconstructing a missing temporary historical test executable, acceptance
  passed without a source fix, including rollback to accepted VST-010 and back.
  Accepted hosted image
  `sha256:19d2207d514aee4dbe468b846485c54255356b994fe0434b4a66ce1838d94dc`
  was deployed. Before/after assertions proved identical mounts and service
  identity; `VIBESTACK_CONTAINER=vibestack-codespaces bin/vibestack-dev check`
  passed 74 checks with zero failures. Hosted logs are below
  `/vibestack-runtime/vibestack/`: `source-vst013.log`, `build-vst013.log`,
  `accept-vst013.log`, `deploy-vst013.log`, `live-vst013.log`.
- Docker publication for the release failed in its stdio project-command fixture
  ([run](https://github.com/check-the-vibe/vibestack/actions/runs/35664704855)).
  GitHub's different host UID left the disposable `/projects` root outside the
  API's required ownership. The acceptance helper now prepares just that mount
  root as `vibe`; it preserves the host-owned sentinel and nested contents and
  does not change runtime ownership checks. Full source, tagged build and full
  acceptance passed after the fixture correction, including 50 browser tests
  and both rollback transitions. Logs: `/tmp/vst013-fixture-{source,build,accept}.log`;
  image `sha256:bac7b59a5fbfaeeb73ae3d31aab5e9664afa1971d844c3f936bb6603867fe545`.
  GitHub confirmation of this correction remains required; no GHCR publication
  is claimed from the successful native client release.

- 2026-09-21: Created during [VST-004](VST-004.md). Entry decisions: Selected protocol/client matrix and operation limits from SPEC-003; no compliance claim from SDK version alone.

- 2026-09-21: Aligned planned scope/dependencies with the simplified SPEC-001 service host; retired machine-broker prerequisites. No implementation claimed.

## Handoff

- Next action: verify the corrected disposable fixture on GitHub, then integrate
  the validated parent stack under the user's direct-merge authorization.
- Gateway-token use and human desktop/provider sign-ins remain separate handoffs.
- Delivery: 0.3.1 client assets are published and the accepted VST-013 runtime is
  deployed in the existing Codespace. The branch is not yet merged into main;
  Docker registry publication remains pending its distinct CI acceptance gate.
