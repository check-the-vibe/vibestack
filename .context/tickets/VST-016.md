# VST-016 — Complete legacy-UI removal and provider-preserving migration

- Phase: in_review
- Specification: [SPEC-004](../specifications/SPEC-004-chat-agent-interface.md)
- Initiative: [Agent platform](../initiatives/agent-platform.md)
- Owner: Codex
- Branch manifest: [VST-016](../branches/VST-016.md)
- Depends on: [VST-015](VST-015.md), [VST-013](VST-013.md)
- Updated: 2026-09-22

## Outcome and scope

Remove legacy navigation, menus, Apps/Settings/setup pages and unused embedded editor/terminal routes/services, including their image/health/assets/docs dependencies. The final entry point is the agent icon and chat overlay. Preserve desktop rendering/automation needed by the canvas and providers, source editing through Codespaces, data, provider selections, credentials and supported session state. Ship a tested rollback path.

## Acceptance

- [ ] SPEC-004 AC-06: verify the ticket-owned portion below and link the result.
- [ ] Documentation, compatibility behavior and exact validation evidence match the implemented change.

## Development plan

1. Read the specification and dependencies; resolve the decisions listed below before dependent work.
2. Inspect source and capture the current contract; implement only this ticket's bounded outcome.
3. Run focused checks, then the applicable full repository gates for source/runtime changes.
4. Record acceptance evidence and remaining limits; integrate under the user’s direct-push/merge authorization, without a PR.

## Verification scope

Inventory old routes and image services; verify no old UI or unauthenticated setup/control mutation remains reachable. Test fresh creation, image upgrade, stop/resume and rollback with repository mounts, provider installation selections/auth/session data and required desktop services intact. Verify in a real Codespace and disposable acceptance images.

## Evidence

Parent `09cfc305266812f36c30cde82522ae46a16bfa8e` passed the full source
baseline before editing (`/tmp/vst016-source-before.log`, exit 0). Its automated
setup/overlay/protocol journeys passed; final image rollback and signed-in real
provider journeys remain VST-014/VST-015 gates. This isolated child implements
removal without deploying it ahead of those gates.

The implementation removes code-server, ttyd, their Supervisor/health/control
entries and build installers, old setup HTML/CSS/JS, obsolete navigation assets
and the native browser-editor shortcut. Old safe bookmarks redirect to the agent
canvas; mutations/assets/WebSockets are rejected. Setup APIs, desktop services,
source bind and all persistence mappings remain. Guides and runner discovery no
longer advertise retired services. The development patch helper refuses to use
an asset patch for the service-removal migration.

The full source gate passed after these changes (`/tmp/vst016-source-final.log`,
312 Python tests plus Go, browser-privacy, syntax and contract checks; exit 0).
Focused service/runner/CLI and infrastructure tests also passed. Tagged candidate
build and expanded full disposable acceptance are in progress. The new gate
checks the actual OpenCode native session and protected data sentinels through
container restart, previous-image rollback and return, without any human account
or model prompt. These outcomes are not claimed until the gate finishes.

The first image passed 30 browser checks and preserved provider metadata/native
session data across restart and rollback, but returning to the clean candidate
left Codex failed with `connection_lost`. A diagnostic repeat reproduced it.
The setup service opens its listener after initial restore inspection; the
provider manager previously failed its first disconnected state read. The repair
retries only GET state reads within the existing activation deadline, preserves
immediate failure for invalid state, and never replays a lost install POST.
Regression tests reproduced the old failure and now pass, including cancellation
and uncertain-submission cases. The complete source gate also passes
(`/tmp/vst016-source-recovery.log`). The repaired tagged image
`vibestack:vst-016-startup-recovery-20260922`,
`sha256:7515ee42759d16abcb21d64fdc917cb45a8e03209d77f716ade16eb12c45d6a8`,
passed the complete disposable gate (exit 0,
`/tmp/vst016-accept-recovery.log`; build `/tmp/vst016-build-recovery.log`).
All 30 browser checks passed. Automatic activation passed both after restart and
after returning from VST-013, preserving provider metadata, the native OpenCode
session, protected data sentinels, project bytes/ETags, jobs, credentials, password,
saved catalog selections and desktop readiness. This used no human account or
model prompt. Final hosted migration and signed-in account continuity remain
VST-007/human checks; the failed candidate was never deployed.

## Decisions and progress

- 2026-09-21: Created during [VST-004](VST-004.md) for the earlier chat-shell plan. Rescoped before implementation to the user's agent-overlay and in-VibeStack provider activation requirements.
- 2026-09-21: Entry decisions: Complete VST-014/VST-015 replacement journeys before removal; retain only desktop services used by the agreed overlay and provider capabilities.

## Handoff

- Branch: `codex/vst-016-legacy-removal`, stacked on VST-015 at `09cfc30`.
- Next action: finish the combined VST-007 hosted gate and preserving deployment,
  then complete the shared human provider/overlay verification.
- Preserve native desktop/provider launches, setup APIs, all persistent data and
  the source bind. Existing code-server data remains available for rollback.
- Delivery: automated local candidate accepted; not merged or deployed. Real Codespaces and
  signed-in provider verification remain required before acceptance is complete.
