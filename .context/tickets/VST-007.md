# VST-007 — Codespaces integration and service-host acceptance

- Phase: in_progress
- Specification: [SPEC-001](../specifications/SPEC-001-service-host.md)
- Initiative: [Agent platform](../initiatives/agent-platform.md)
- Owner: Codex
- Branch manifest: [VST-007](../branches/VST-007.md)
- Depends on: [VST-009](VST-009.md), [VST-013](VST-013.md), [VST-016](VST-016.md)
- Updated: 2026-09-22

## Outcome and scope

Integrate static hosting and the single API/MCP service into the existing image/Codespaces lifecycle. Prove fresh creation, authenticated client access, extension addition/removal, persistence, upgrade and rollback. Resolve integration failures using the existing local job/persistence contract; no distributed dispatch ledger or database service.

## Acceptance

- [ ] SPEC-001 AC-01: verify the ticket-owned portion below and link the result.
- [x] SPEC-001 AC-02: full disposable authentication/grant/identity/forged-header checks pass.
- [x] SPEC-001 AC-03: executable extension add/remove demonstration passes on all four surfaces.
- [x] SPEC-001 AC-04: full acceptance verifies static isolation through nginx.
- [x] SPEC-001 AC-05: bounded errors, transport faults, restart recovery and privacy checks pass.
- [ ] SPEC-001 AC-06: verify the ticket-owned portion below and link the result.
- [ ] Documentation, compatibility behavior and exact validation evidence match the implemented change.

## Development plan

1. Read the service-host specification and shared capability contract.
2. Implement this bounded increment, preserving existing private clients/data.
3. Run focused checks and the repository gates required for source/runtime changes.
4. Record evidence for this ticket's part of each criterion; integration criteria remain open until tested with real clients.

## Verification scope

Fresh Codespace from the exact candidate; real guide-only CLI/MCP access with gateway steps; authenticated browser HTTP invocation; extension demo; nginx static/legacy-route isolation; restart, lost responses and rollback preserving credentials/source/data. No complete claim based only on unit tests or reused hosted state.

## Evidence

The full inherited source baseline passed before edits at `5308870`
(`/tmp/vst007-source-before.log`, exit 0). Parent VST-016 acceptance found a
provider auto-activation failure after rollback; its subsequent repair and
complete passing gate are inherited and recorded below.

The extension authoring probe builds a harmless new module in an isolated source
copy, rejects duplicate/invalid compiled candidates before listening, adds the
valid binary only to a verified disposable acceptance container, and restores
the original binary. It exercises discovery, schema, invocation and denials
through nginx REST, the generic CLI, MCP and a real Chromium session before,
during and after the addition. Source authentication and nginx remain unchanged.
The focused demonstration passed in a new disposable container (exit 0), including
invalid candidate startup and the complete add/remove sequence. The full source
gate passed again after merging the parent startup repair at `cfae5b0`
(`/tmp/vst007-source-final.log`); the tagged integration image built successfully.
Combined full image acceptance passed (exit 0, `/tmp/vst007-accept-final.log`),
including the extension demonstration, real catalog installation, REST/MCP/CLI
parity and transport recovery, all 30 browser checks, and restart/rollback/return
preserving automatically activated providers, native OpenCode session metadata,
identity, grants, project bytes/ETags, jobs, password and selected applications.
Accepted local image: `vibestack:vst-007-integration-20260922`,
`sha256:d115a5bde745e26a6e22a5706885ed5a391be2b2f745cf9b754e7667de142634`.
The build log is `/tmp/vst007-build-final.log`. No native model account or prompt
was used for these gates.

Fresh Codespace `congenial-space-telegram-qv7995r69wc4pg9` was created through
Chrome from main `4894b2c5d15cce149401b0ed3bbab0b9f599eae5`, using GitHub's default
eligible 4-core selection. The terminal measured four CPUs and 16,377,436 kB RAM.
The runtime uses the named `vibestack-runtime-...` volume; the Ports table confirms
8080 is Private. Automatic startup completed with no manual start command.
The live check passed 74/0. `/status.txt` and the 29,457-byte `/AGENTS.md` were
retrievable; README SHA-256 matched between the outer checkout and its nested
`/projects/vibestack` bind. The exact `/data`, `/projects` and source mounts were
retained. Baseline logs live under `/vibestack-runtime/vibestack/fresh-main-*`.

That same Codespace then switched cleanly to the integration branch at
`27fde749117a824679b6bcbee6c04dd97965860b`; no new Codespace or data migration was
needed for the branch switch. Its independent source/build/full acceptance run
passed (gate exit 0), including all 30 browser checks and the extension lifecycle.
The accepted image was deployed through the existing lifecycle helper (deployment
exit 0): `sha256:fdf9398ca66b91735c7a9a0f735be343ae4110e944e37ff99813b38a7e995d98`.
Docker reports healthy and the live gate passed 88/0. The three original mounts
remain intact. Chrome shows the final agent icon and credential form.

The Codespaces-local MCP source probe also passed status, argv/output, a
conditional file update visible in the real Git checkout, stale-write denial
and screenshot capture. It ran against its own disposable container/credential
and removed the probe file. Hosted logs are under `/vibestack-runtime/vibestack/`:
`{source,build,accept}-vst007.log`, `source-mount-vst007.log`, `deploy-vst007.log`,
`live-vst007.log` and `runtime-vst007.txt`. Source remained clean afterward.
This is a fresh baseline plus accepted final-image upgrade, not a new Codespace
created at the exact final source. Live browser authentication, external private
gateway access, hosted stop/resume/outer rebuild and signed-in provider turns
remain separate, unclaimed checks.

GitHub publication [run 35676457384](https://github.com/check-the-vibe/vibestack/actions/runs/35676457384)
passed source/build, then failed twice when the extension fixture's temporary
replacement exited 127 under Supervisor; the original service was restored.
The fixture now targets the inspected image's Linux architecture, disables CGO
like the production Docker build, and sets root ownership/0555 executable mode
explicitly. It no longer relies on the host build or copy attributes. The full
source, tagged build and disposable acceptance passed after this correction,
including extension parity, 30 browser tests and both rollback transitions.
Logs: `/tmp/vst007-source-ci-final.log`, `/tmp/vst007-build-ci-final.log`,
`/tmp/vst007-accept-ci-final.log` (exit 0). Image:
`vibestack:vst-007-ci-portable-20260922`,
`sha256:0693bd20de3ee6fa958886f1793afa292ab212137e2df30c5572acb276f0d51f`.
This changes test-binary construction and its guide, not runtime service code.
The corrected [GitHub run 35678433512](https://github.com/check-the-vibe/vibestack/actions/runs/35678433512)
passed source, build, full acceptance and GHCR publication at `f5ea1bc`.
Congenial Space Telegram independently passed its hosted source/build/full gate,
then deployed `sha256:7890acabbe508fb613b14aa2c9cf1197b590d5268a52f254a4615d979bce7b27`.
The live check passed 88/0 and retained all three mounts. Hosted evidence uses
`{source,build,accept,deploy,live,runtime}-vst007-ci` below the runtime directory.

A second fresh Codespace, `ubiquitous-space-system-4qjggp4w762jqj`, was created
directly from integrated main `00f49250c9cd8ff82006664f9e1cf9c9e18fb03e` in Chrome.
GitHub selected four cores; the terminal confirmed four CPUs/16,377,436 kB RAM.
Automatic image build/start completed without a manual start command. The live
check passed 88/0 with image
`sha256:8711bfe058f5eccda339222eb2423e3948cec4970e7b35f9bf55e9997336edc8`.
Port 8080 was inspected as Private, all three expected mounts were present, and
the host/shared README hashes matched. A protected baseline records volume and
witness, service identity, root-private key hashes/ownership/modes, mounts and
Git source for the actual platform stop/resume/rebuild checks. No account or
application probe credential was added to this live workspace.
This closes the literal fresh-integrated-source startup gap; the later `f5ea1bc`
change affects only the test fixture and its guide.

Actual Codespaces stop/resume exposed a separate desktop boot defect: `/tmp/.X0-lock`
and the X0 socket survived an abrupt platform stop, so Xvfb refused to start even
though no X server remained. The protected comparison confirmed unchanged volume,
witness, service identity, key bytes/owners/modes and Git source. The initial
resume did not pass desktop health. The validation container was stopped while
the source repair is accepted; the separate Congenial deployment remains healthy.

Boot now clears only checked display-0 temporary state before Supervisor starts.
Cleanup uses no-follow directory descriptors and rejects unsafe types, links,
ownership and filesystem crossings. Three new regression methods cover PID reuse,
real Unix sockets, symlink targets, hard links, unexpected files and other-display
preservation; the 13-test bootstrap suite passes. Full source checks pass; the
tagged image and expanded full acceptance are running. The expanded gate forcibly
kills its disposable container with a stale lock, then verifies desktop health
and provider restoration before the existing rollback transitions. A new real
Codespaces restart/rebuild result is still required after accepted deployment.
The first full run passed all 30 browser checks and normal provider restart,
but its new stale-lock fixture used `open(..., "w")`; Linux's sticky-directory
`protected_regular` policy rejected the implied O_CREAT on the vibe-owned lock.
The fixture now opens the existing file without O_CREAT or symlink following,
checks its descriptor identity, and writes the test PID through that descriptor.
This passed a network-disabled disposable-container probe. The boot cleanup itself
was unchanged; the corrected full gate must finish before deployment.

## Decisions and progress

- 2026-09-21: Created during [VST-004](VST-004.md), initially for broker dispatch recovery and deployment.
- 2026-09-21: Rescoped before implementation after the user replaced the fleet-broker design with a static/API/MCP service host. The previous scope is retired, not completed. Entry decisions: SPEC-001 D1 supported-client access path and D3 measured limits; dependency evidence must be complete.

## Handoff

- Remaining work: accept/deploy the stale-X11-state repair and repeat hosted lifecycle checks,
  authenticated remote-client verification and human provider checks.
- Next action: finish the local/hosted candidate gates, preserving deployment and
  actual stop/resume/rebuild, then follow the [runthrough](../runthrough.md). Keep accounts and
  credentials outside chat. Remote private-gateway verification awaits the
  requested four-hour `project_summary` credential authorization.
- Branch: `codex/vst-007-codespaces-integration`, stacked on VST-016 at `5308870`.
- Delivery: implementation merged directly to main at `00f4925`, with user
  authorization, and accepted integration image deployed to the new Codespace.
  Final acceptance remains open; merging and deployment do not close human checks.
