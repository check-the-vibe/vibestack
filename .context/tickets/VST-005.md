# VST-005 — Static host and authenticated workspace service

- Phase: in_review
- Specification: [SPEC-001](../specifications/SPEC-001-service-host.md)
- Initiative: [Agent platform](../initiatives/agent-platform.md)
- Owner: Codex
- Branch manifest: [VST-005](../branches/VST-005.md)
- Depends on: [VST-008](VST-008.md)
- Updated: 2026-09-21

## Outcome and scope

Add the single workspace application process and nginx/static routing. Centralize authenticated dispatch for API and MCP adapters, use protected persistent credentials, and reserve the guide/discovery paths. Protect or retain private-only legacy routes; do not introduce anonymous pairing, a fleet registry or a new identity server. MCP client transport details belong to VST-011/VST-012.

## Acceptance

- [ ] SPEC-001 AC-01: verify the ticket-owned portion below and link the result.
- [ ] SPEC-001 AC-02: verify the ticket-owned portion below and link the result.
- [ ] SPEC-001 AC-04: verify the ticket-owned portion below and link the result.
- [ ] Documentation, compatibility behavior and exact validation evidence match the implemented change.

## Development plan

1. Add one loopback Go process and supervised image packaging. Preserve the existing execution services and private desktop transport.
2. Add protected, instance-bound credentials with local issuance/revocation, compatibility with existing workspace tokens, and a shared principal resolver. Browser sessions require a human credential handoff and per-request revocation/CSRF checks.
3. Move legacy API aliases through authenticated adapters; block anonymous credential issuance and unknown API fallthrough. Keep the separate host runner outside this boundary.
4. Serve only the configured static publish root; reject hidden files, traversal, symlink escapes, unsafe methods and directory listings. Reserve guide/discovery and future MCP/capability routes.
5. Test authentication, file permissions, cross-instance/Origin failures, revocation, body/output limits and actual nginx routing. Adapt existing acceptance clients to authenticate rather than weakening the boundary.
6. Build and accept a disposable image before any Codespace replacement. Record the ticket-owned foundation checks; live MCP and guide/client acceptance remain in dependent tickets.

## Verification scope

Published-file boundaries through nginx; invalid/missing credentials before dispatch; internal listeners; compatibility-route bypass attempts; restart with existing protected credentials. Record API/MCP end-to-end parts pending until their adapter tickets complete.

## Evidence

The Go service, protected credential store, browser session exchange, nginx
authentication boundary and explicit static publish directory are implemented.
Legacy API paths pass through bounded adapters; old anonymous pairing is no
longer a bootstrap path. `/mcp` is explicitly unavailable after authentication
until VST-011. This ticket does not claim MCP tool or hosted acceptance.

- Before/after source gate: 312 Python tests, all Go packages, three JavaScript
  privacy tests, syntax/JSON/whitespace checks pass. Logs:
  `/tmp/vst-005-before.log`, `/tmp/vst-005-final-source.log`.
- Focused Go checks cover credential expiry/revocation, concurrent issuance,
  restrictive files, cross-instance grants, CSRF, origin and proxy spoofing,
  bounded adapters and static publication failures.
- First image acceptance exposed a legacy error-envelope regression; fixed the
  compatibility aliases and added a regression test. The subsequent runtime
  check passed 94 checks, including automation, before package-restoration tests.
- Final candidate `vibestack:vst-005-final-20260921` built successfully, manifest
  `sha256:1cf0848963d93dac0b7075dff4c8228a57daf27a167be6dd74b77e40d145fe47`.
  Full acceptance is running on isolated loopback port 18322, including explicit
  nginx static-negative checks and browser credential exchange. Log:
  `/tmp/vst-005-accept-final.log`. A running check is not a pass.

No live container or Codespace has been replaced. Combined real Codespaces
acceptance belongs to VST-007 after the dependent transport and client tickets.

## Decisions and progress

- 2026-09-21: Claimed under the active request to implement all remaining specifications, deploy and provide a verification runthrough. Keep the existing trusted-owner workspace model and protected file storage; no OAuth issuer, external database or machine broker is introduced by this foundation.

- 2026-09-21: Created during [VST-004](VST-004.md), initially for machine registry, grants and enrollment.
- 2026-09-21: Rescoped before implementation after the user replaced the fleet-broker design with a static/API/MCP service host. The previous scope is retired, not completed. Entry decisions: SPEC-001 D2 layout and D3 static path/limits; coordinate the shared authentication interface with SPEC-003 D1.

## Handoff

- Remaining work: finish disposable acceptance and resolve any failures.
- Next action: inspect the final acceptance result, then record exact evidence before integration.
- Branch: `codex/vst-005-workspace-service`, based on main after VST-008.
- Uncommitted work: inspect Git status; implementation and evidence belong to this ticket.
- Delivery: candidate in review; no new service deployed.
