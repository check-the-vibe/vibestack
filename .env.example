# Example Environment File for Docker Container Configuration
# Copy this file to .env and fill in your values

# Git Configuration (Required for git operations)
GIT_USER_NAME=Your Full Name
GIT_USER_EMAIL=your.email@example.com

# API Keys (Optional - for LLM integrations)
OPENAI_API_KEY=sk-your-openai-api-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# Container Configuration (Optional)
# Host HTTP port mapped to Nginx in the container (80)
HOST_HTTP_PORT=3000
ROOT_PASSWORD=
VIBE_PASSWORD=coding
# Default Codex callback port (host<->container)
CODEX_CALLBACK_PORT=1455

# Optional: Map a host Projects folder into the container at /projects
# Default (if unset) is "$HOME/Projects"
# Example:
# HOST_PROJECTS_DIR=/Users/you/Projects
# HOST_PROJECTS_DIR=C:\\Users\\you\\Projects
# HOST_PROJECTS_DIR=
